type Query {
    allUsers(name: String): [User]
}

type User {
    _id: ID
    userName: String
    gender: String
    imageLink: String
    active: Boolean
    contact: String
    password: String
    order: Order
}